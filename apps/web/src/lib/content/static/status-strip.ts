import type { StatusStrip } from "@portfolio/types";

// Placeholder pending real CI/deploy wiring (F4) — same interim state the
// mockup itself is in. Honest right now: this genuinely is the repo's
// current CI status.
export const statusStrip: StatusStrip = {
  build: "passing",
  lastDeploy: "2026-09-24",
};
